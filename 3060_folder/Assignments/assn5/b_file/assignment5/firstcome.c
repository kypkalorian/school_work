#include "functions.h"


void firstCome(struct process *structArray){

    int clock = 0;

    //testing this
    printf("this is a value from first come: %d\n", structArray[10].processingTime  );

    while(0){

        //check for arrivals
            //add arrival to ready queue

        //decrement current process remaining time
        //increment the ready queue wait time

        //if process finishes, remove it from ready queue

        clock++;
    }



}
